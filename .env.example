SECRET_KEY=fittrack123
DEBUG=True

DB_NAME=fittrack
DB_USER=fittrack
DB_PASSWORD=fittrack
DB_HOST=localhost
DB_PORT=5432
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379/0
FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json

# E-mail (password reset)
# Dev: backend console já é o padrão, não precisa configurar
# Prod: trocar pelo backend SMTP ou SendGrid
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@fittrack.app
# URL base para o link de reset (mobile deep link ou URL do painel web)
PASSWORD_RESET_URL_BASE=https://fittrack.app/reset-password